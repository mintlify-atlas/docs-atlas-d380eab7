---
title: "BitUnlocker: BitLocker Downgrade Attack PoC (CVE-2025-48804)"
sidebarTitle: "Home"
description: "Proof-of-concept downgrade attack accessing BitLocker-encrypted disks on fully patched Windows 11 via a signed pre-patch boot manager."
---

BitUnlocker is a proof-of-concept that demonstrates how BitLocker-encrypted disks on fully patched Windows 11 machines can be accessed **in under 5 minutes** by exploiting a boot manager downgrade attack. The vulnerability, documented as **CVE-2025-48804**, affects systems whose Secure Boot database still trusts the Microsoft Windows PCA 2011 certificate. The July 2025 patch fixes `bootmgfw.efi`, but any pre-patch binary signed under PCA 2011 can be used to load a tampered WinRE environment — bypassing TPM-sealed BitLocker keys without a recovery password.

<Warning>
  This tool is provided strictly for **authorized security testing and research**. Only use it on systems you own or have explicit written permission to test. Unauthorized access to computer systems is illegal.
</Warning>

<CardGroup cols={2}>
  <Card title="How It Works" icon="circle-info" href="/how-it-works">
    Understand the downgrade attack chain, the SDI vulnerability, and why pre-patch boot managers expose the BitLocker VMK.
  </Card>
  <Card title="Prerequisites" icon="list-check" href="/prerequisites">
    Physical access, Secure Boot trust requirements, and the hardware/software you need before starting.
  </Card>
  <Card title="USB Boot (Recommended)" icon="usb-drive" href="/walkthrough/usb-boot">
    The simplest delivery method: format a FAT32 USB stick and trigger a UEFI boot in under 5 minutes.
  </Card>
  <Card title="PXE Boot" icon="network-wired" href="/walkthrough/pxe-boot">
    Network-based delivery using dnsmasq over Ethernet — useful when USB ports are restricted.
  </Card>
  <Card title="Build a Custom SDI" icon="wrench" href="/tools/patch-sdi">
    Use patch_sdi.py to append your own WinRE.wim to a stock boot.sdi and redirect the WIM blob pointer.
  </Card>
  <Card title="Mitigations" icon="shield-halved" href="/defense/mitigations">
    How to protect against this attack: TPM+PIN, CA 2023 migration, KB5025885, and PCR policy hardening.
  </Card>
</CardGroup>

## Quick overview

<Steps>
  <Step title="Obtain boot_patched.sdi">
    Download the pre-built `boot_patched.sdi` from the GitHub Releases page, or [build your own](/tools/patch-sdi) from a stock `boot.sdi` and a modified `WinRE.wim`.
  </Step>
  <Step title="Modify the BCD on the target">
    Open a WinRE command prompt on the target machine and use `bcdedit` to redirect the ramdisk SDI path to `\sdi\boot_patched.sdi`. See [Modify BCD](/walkthrough/modify-bcd) for exact commands.
  </Step>
  <Step title="Boot the target from your device">
    Use [USB boot](/walkthrough/usb-boot) (recommended) or [PXE boot](/walkthrough/pxe-boot) to load the pre-patch `bootmgfw.efi` and the modified BCD.
  </Step>
  <Step title="Access the decrypted volume">
    Once the SDI transfer completes, a command prompt appears with the OS volume decrypted and mounted — typically at `C:` or `E:`.
  </Step>
</Steps>

## About this research

This work builds entirely on original research by **Microsoft STORM** (Netanel Ben Simon and Alon Leviev). See the [Credits](/defense/credits) page for attribution and links to the upstream disclosure.
